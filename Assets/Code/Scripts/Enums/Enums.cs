public enum PlayMode
{
    FirstPerson,
    ThirdPerson
}

public enum GameState
{
    Start,
    FirstPerson,
    ThirdPerson,
    Win,
    Lose
}

public enum BrickType
{
    Normal,
    Explosive,
}